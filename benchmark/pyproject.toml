[project]
name = "benchmark"
version = "0.1.0"
description = "Load testing tool for GeoFleet Nearby Search Service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "redis>=8.0.1",
]
